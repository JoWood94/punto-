// Placeholder — App root bootstrap tests.
// Il componente root richiede TranslateService e Firebase: mocking completo in futuro.
describe('App', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
