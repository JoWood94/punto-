// Placeholder — LoginComponent unit tests da scrivere in futuro
// (dipendenze Firebase richiedono mocking completo)
describe('LoginComponent', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
