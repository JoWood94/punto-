// Placeholder — NoteEditorComponent unit tests da scrivere in Fase 1
// (il componente ha dipendenze Firebase che richiedono mocking completo)
describe('NoteEditorComponent', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
