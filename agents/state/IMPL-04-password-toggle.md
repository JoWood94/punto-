status: done
agent: alpha
task: Login — bottone mostra/nascondi password
completato: Aggiunta proprietà `showPassword = false` in LoginComponent (login.ts). In login.html, sostituito il campo password con versione che usa `[type]="showPassword ? 'text' : 'password'"` e aggiunto `mat-icon-button` con `matSuffix` che toglla showPassword e mostra l'icona visibility/visibility_off con aria-label in italiano.
bloccato_da:
